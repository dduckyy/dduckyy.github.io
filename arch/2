find /usr/share/zoneinfo
read -p "Enter timezone (format: Zone/SubZone): " time
ln -sf /usr/share/zoneinfo/$time /etc/localtime
hwclock --systohc
nano /etc/locale.gen
locale-gen
echo "LANG=" >> /etc/locale.conf
nano /etc/locale.conf
read -p "Choose system name: " name
echo "$name" >> /etc/hostname
echo "127.0.0.1 localhost" >> /etc/hosts
echo "::1       localhost" >> /etc/hosts
echo "127.0.1.1 $name.localdomain $name" >> /etc/hosts
mkinitcpio -P
passwd
pacman -S celluloid curl efibootmgr eog evince file-roller gdm gedit git gnome-backgrounds gnome-control-center gnome-keyring gnome-shell gnome-system-monitor gnome-terminal gnome-tweak-tool grub gvfs-goa gvfs-google nautilus networkmanager-openvpn noto-fonts noto-fonts-cjk noto-fonts-emoji noto-fonts-extra nvidia nvidia-settings pacman-contrib pulseaudio sudo xdg-user-dirs
mkdir /boot/efi
read -p "Enter boot partition: " boot
mount $boot /boot/efi
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=GRUB
nano /etc/default/grub
nano /etc/mkinitcpio.conf
mkinitcpio -P
grub-mkconfig -o /boot/grub/grub.cfg
nano /etc/pacman.conf
read -p "Enter username: " uname
useradd -m -G wheel $uname
passwd $uname
EDITOR=nano visudo
systemctl enable gdm
systemctl enable NetworkManager
systemctl enable bluetooth
echo Install finished! Type \"exit\", then \"reboot now\".
